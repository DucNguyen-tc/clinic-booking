server:
  port: 8086

spring:
  application:
    name: clinic_notification

  datasource:
    url: jdbc:mysql://localhost:3306/clinic_notification?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: houyen080@gmail.com
    password: vwtb gkxn yfrc ujvk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  mail:
    from-name: "Phòng Khám Online"
    from-email: houyen080@gmail.com
