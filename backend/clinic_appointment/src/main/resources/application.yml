server:
  port: 8083

spring:
  application:
    name: clinic_appointment
  datasource:
    url: jdbc:mysql://localhost:3306/clinic_appointment?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

